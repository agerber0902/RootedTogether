import { StyleSheet } from "react-native";

export const appBootstrapStyle = StyleSheet.create({
  loadingContainer: {
    flex: 1,
    justifyContent: "center",
    alignItems: "center",
  },
  spinnerContainer: {
    padding: 0,
  },
});
